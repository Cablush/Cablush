<%=render "layouts/menu_cadastro" %>
<div class="content-box">
    <div class="wrap">
        <ul class="events">
            <li class="active"><a href="#">Eventos</a></li>
            <li><%= link_to 'Novo Evento', new_evento_path %></li>
        </ul>
        <div class="clear"></div>
    </div>
</div>
<div class="main">
    <div class="wrap">
        <ul>
            <% @eventos.each do |evento| %>
              <li>
                  <%= link_to evento.nome, edit_loja_path(evento) %>
              </li>
            <% end %>
        </ul>        
    </div>
    <div class="clear"></div>
</div>
<%=render "layouts/footer"%>