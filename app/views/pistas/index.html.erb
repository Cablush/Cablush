<%=render "layouts/menu_cadastro" %>
<div class="content-box">
    <div class="wrap">
        <ul class="events">
            <li class="active"><a href="#">Pistas</a></li>
            <li><%= link_to 'Nova Pista', new_pista_path %></li>
        </ul>
        <div class="clear"></div>
    </div>
</div>
<div class="main">
    <div class="wrap">
        <ul>
            <% @pistas.each do |pista| %>
              <li>
                  <%= link_to pistas.nome, edit_loja_path(pista) %>
              </li>
            <% end %>
        </ul>        
    </div>
    <div class="clear"></div>
</div>
<%=render "layouts/footer"%>